node_modules/
.cache/
public
.claude/

# docs/ is internal source material for the site (grant proposal, donor
# figures, biosketch, unpublished manuscripts). The repo is PUBLIC, so keep
# the entire folder local-only. The Gatsby build reads only src/, never docs/.
# To share a specific non-sensitive file, force-add it: git add -f docs/<file>
docs/
